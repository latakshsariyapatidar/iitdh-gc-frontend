# When deploying to Vercel, leave this empty to use the Next.js rewrite proxy (avoids Mixed Content errors)
# Local development can still use the full URL if needed, or rely on the rewrite if running next dev
NEXT_PUBLIC_API_URL=
